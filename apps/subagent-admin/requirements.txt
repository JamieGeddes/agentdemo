# Account-admin subagent (Python + Google ADK, exposed over A2A).
# Install into a local venv with: npm run setup:admin   (Python >= 3.10)
#
# The [a2a] extra pins a compatible a2a-sdk (the 0.3.x line, matching the
# @a2a-js/sdk 0.3.x the main agent uses). Do NOT depend on a2a-sdk>=1.x — ADK
# needs `a2a.server.apps`, which the 1.x rewrite removed.
google-adk[a2a]>=2.0.0
# a2a-sdk's JSON-RPC Starlette app needs starlette + sse-starlette (its
# [http-server] extra). uvicorn[standard] brings starlette; add sse-starlette.
sse-starlette>=2.0
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0
pytest>=8.0

